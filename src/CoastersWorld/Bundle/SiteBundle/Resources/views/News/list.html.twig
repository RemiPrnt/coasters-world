{% extends '::base.html.twig' %}

{% block title %}{{ 'news.title.home'|trans }}{% endblock %}

{% block body %}
<div class="row">
    <div class="span8">
        <!-- Content -->
        <div class="content">
            <!-- Blog -->
            <section class="blog">
                <div class="page-header">
                    <h1>Liste des news</h1>
                </div>
                {% for news in listNews %}
                    <div class="item">
                        <div class="row">
                            <div class="span3">
                                <figure>
                                    <a href="single.html">
                                        <img src="http://placehold.it/270x182" alt="">
                                        <div class="icon">
                                            <i class="icon-play"></i>
                                        </div>
                                    </a>
                                </figure>
                                <div class="comments-block">
                                    <small><i class="icon-play-circle"></i> Video</small>
                                    <div class="pull-right">
                                        <small><i class="icon-comments"></i> {% transchoice news.comments|length with {'%count%': news.comments|length} %}news.comments{% endtranschoice %}</small>
                                    </div>
                                </div>
                            </div>
                            <div class="span5">
                                <h3>
                                    <a href="{{ path('coasters_world_news_view', {'slug': news.slug})  }}">
                                        {{ news.title }}
                                    </a>
                                </h3>
                                <p class="meta">
                                    <i class="icon-time"></i> {{ news.publishedAt|date("d/m/Y") }} | <i class="icon-user"></i> by <a href="single.html"><em>{{ news.author.username }}</em></a>
                                </p>
                                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ipsam inventore dicta velit tempore alias labore aut iusto fuga non suscipit? Magni cum perspiciatis qui hic cupiditate possimus voluptates enim asperiores rerum doloremque similique amet? Amet ex fuga quo hic quibusdam.</p>
                                <a href="{{ path('coasters_world_news_view', {'slug': news.slug})  }}"><i class="icon-circle-arrow-right"></i> {{ 'news.read.more'|trans }}</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}

                {# display navigation #}
                {{ knp_pagination_render(listNews) }}
            </section>
        </div>
    </div>
</div>
{% endblock %}
